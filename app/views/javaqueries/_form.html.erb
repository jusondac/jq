<%= form_with(model: javaquery, local: true) do |form| %>
  <% if javaquery.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(javaquery.errors.count, "error") %> prohibited this javaquery from being saved:</h2>

      <ul>
      <% javaquery.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :code %>
    <%= form.text_area :code %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
